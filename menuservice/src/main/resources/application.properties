spring.application.name=menuservice
server.port=8082

management.endpoints.web.exposure.include=health,prometheus
management.metrics.tags.application=${spring.application.name}
management.metrics.distribution.percentiles-histogram.http.server.requests=true

# Server mTLS
server.ssl.enabled=true
server.ssl.certificate=file:/certs/menuservice.crt
server.ssl.certificate-private-key=file:/certs/menuservice.key
server.ssl.client-auth=need
server.ssl.trust-certificate=file:/certs/ca.crt

# Management port zonder TLS (voor Prometheus scraping)
management.server.port=18082
management.server.ssl.enabled=false
